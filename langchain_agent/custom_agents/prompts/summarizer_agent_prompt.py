SUMMARIZER_AGENT_DESCRIPTION_AS_TOOL = (
    "Summarises long text into a concise paragraph and extracts the key points. "
    "Provide the raw text and the agent will return a structured summary."
)

SUMMARIZER_AGENT_SYSTEM_PROMPT = (
    "You are a text summarisation expert. "
    "Produce a concise summary and extract the key points from the given text. "
    "Always respond using the structured output format."
)
